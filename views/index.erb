<!DOCTYPE html>
<html>
  <head>
    <title>RPS</title>
  </head>

  <body>
    <h1>Rock Paper Scissors!</h1>

    Computer score: <%= session[:comp_score] %><br>
    Player score: <%= session[:player_score] %><br><br>

    <% if @choice %>
      Player choice: <%= params[:choice] %><br>
      Computer choice: <%= @comp_choice %><br><br>
      <%= @result %><br><br>
    <% end %>


    <% if session[:player_score].to_i < 2 && session[:comp_score].to_i < 2 %>
      <form action="/" method="post">
        <input type="submit" name="choice" value="rock">
        <input type="submit" name="choice" value="paper">
        <input type="submit" name="choice" value="scissors">
      </form><br>

    <% elsif session[:player_score].to_i > 1 %>
        Player wins the game!
        <form action="/reset" method="post"><br>
          <input type="submit" value="Play again!">
        </form>
    <% elsif session[:comp_score].to_i > 1 %>
        Computer wins the game!
        <form action="/reset" method="post"><br>
          <input type="submit" value="Play again!">
        </form>
    <% end %>

  </body>
</html>
